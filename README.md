## UniSon - Unisis Management Module

# Requirements:

Packages: (see requirements.txt)
- sudo pip install spur (SSH library)
- sudo pip install putty-tools (PuTTY library)

Utilities:
- doctl (see extras subdirectory)

